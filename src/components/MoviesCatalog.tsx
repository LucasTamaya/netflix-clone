import { MovieRow } from "./MovieRow";

import { moviesRequests } from "../assets/moviesRequests";

export const MoviesCatalog: React.FC = () => {
  return <div>{/* {moviesRequests.fromEntries} */}</div>;
};
